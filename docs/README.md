# 每日日语练习 | Daily Japanese Exercises

一个每天自动生成 5 道日语小练习、外加 5 道动词变形题的应用；两块题下方各有一个「再出题」按钮，各自最多可再加两组。题目全部来自本仓库的手册内容，做错的题会回流到仓库，用来调整后续练习的出题重点。

> **本文档只覆盖练习应用**（`exercise-generator/`、`docs/`、`.github/workflows/exercise-*.yml`）。
>
> 手册内容的维护规范见 [PROJECT_SPEC.md](../PROJECT_SPEC.md)，发布流程见 [RELEASE_WORKFLOW.md](../RELEASE_WORKFLOW.md)。
>
> 练习应用**只读**手册内容，不会修改 `handbook/` 下的任何文件。

---

## 1. 工作方式 | How it works

```text
handbook/*.md
    │   语法、动词、助词、固定表达、词汇、易错点
    │
    │   exercise-generator/itembank.py
    │       解析 Markdown → 结构化题库（含易混淆关系）
    ▼
exercise-generator/generate_exercises.py
    │   按掌握度挑 5 个知识点，再决定题型并生成题目
    │   生成后做质量检查，不合格则整天作废
    ▼
docs/data/exercises/YYYY-MM-DD.json
    │
    ▼
docs/index.html （网页应用）
    │   答题 → 判分 → 生成结果 JSON
    │   提交 GitHub Issue（标签 exercise-result）
    ▼
exercise-generator/ingest_mistakes.py
    │   记录对错 → docs/data/mistakes.jsonl + attempts.jsonl
    │
    └──────► 更新掌握度 → 决定明天出哪些知识点
```

---

## 2. 题型 | Exercise formats

| 题型 | 来源 | 说明 |
|------|------|------|
| 选择题 MCQ | 语法／助词／表达／词汇 | 选出正确的中文含义或读音 |
| 填空题 Fill in the blank | 例句 | 从例句中挖掉整个词（含活用），答案接受汉字或假名；空格较大时给中文提示 |
| 翻译题 Translation | 例句 | 给出中文，写出日语；判分忽略假名标注、空格和标点 |
| 改错题 Correction | `06-Mistakes.md` ／ 你自己的错题 | 给出错误句子，改写成正确说法 |
| 变形题 Conjugation | `02-Verbs.md` V-004 ～ V-007 | 单纯的形式转换已独立成下方的「动词变形练习」区块 |
| 形式判断 Form choice | V-004 动词 ＋ 手册例句 | 只给中文和词类，自己想出是哪个动词、该用哪种形式 |

每天 5 题，题型由「哪些题型对该知识点可行」和「今天已经出过哪些题型」共同决定，因此不会出现 5 题全是选择题的情况。

### 汉字都带假名 | Kanji comes with its reading

出题时**汉字一律带上假名**，**读音题除外**——那道题问的就是读音，注上去等于给答案。

- 填空题、改错题的句子保留手册原本的振假名：`太陽（たいよう）は＿＿＿に沈（しず）みます。`
- 变形题给出的那个形式也注上读音：`「回って（まわって）」（て形）→ 写出ない形：`
- 词条本身按手册的「假名」列显示：`「芸術（げいじゅつ）」的意思是？`
- 语法与固定表达没有「假名」列，就**只用该条目自己例句里已经注过的读音**：G-040 的例句写着 `方（ほう）`，所以标题显示为 `～た方（ほう）がいい`

**读音一律不自造**：要么是手册已经注好的，要么由 `kana_form()` 从「假名」列推导。同一条目里同一个汉字出现两种读法（`方` 既有 ほう 又有 かた）时宁可留白，也不猜。手册已经整词注音的（`～何度も（なんども）`）保持原样，不再逐字重复注一遍。

判分不受影响：作答比对一律先去掉假名注音，写汉字或写假名都算对。

### 动词变形练习（另加 5 题）

5 道正题之后是一个独立的**动词变形**区块，同样每天 5 道。这一块没有例句、没有上下文——给出动词的某一种形式，写出另一种：

```
「歩きます」（ます形）→ 写出可能形：      →  歩ける
「習って」（て形）  → 写出辞书形：        →  習う
「呼ぶ」（辞书形）  → 写出た形：          →  呼んだ
```

- 给出的形式在**辞书形／ます形／て形**之间轮换，目标形式可以是辞书形、て形、た形、ない形、可能形（ます形只在推不出别的形式时才作为目标）
- 答案接受汉字或全假名（`歩ける` 与 `あるける` 都算对）
- 词源是 `02-Verbs.md` V-004 的 100 个动词，加上 `05-Vocabulary.md` 里 62 条带词类的 `W-V` 动词，共 **162 个**；选词同样由掌握度驱动，答错的动词会隔几天回来
- V-004 表自带ます形与て形（`行く→行って` 这类特例写在表里）；`W-V` 没有表，两种形式按词类从辞书形推导。推导规则拿 V-004 的 100 个动词逐条验证过，除 `行く` 外完全吻合，特例另有一份清单兜底
- **不出题的情况**：固定搭配与复合动词（`気をつける`、`手を洗う`、`お腹が空く`、`付いてくる`）整条排除；自动词不出可能形——`始まれる`、`咲ける` 不自然，`付ける`、`並べる`、`入れる` 更会撞上自己的他动词对。这些动词的其余四五种形式照常出题
- 同一个动词不会在同一天既出现在正题里、又出现在变形题里
- 计分与正题分开显示：`得分：4 / 5　·　变形：3 / 5`；两块的结果一起提交，都会进入复习队列
- 用 `--drills N` 调整题量，`--drills 0` 关闭这一块（此时变形题会回到正题的题型池里）
- 这一块也有自己的「再出题」按钮，与正题互不影响

**正题里的动词换成更难的问法。** 单纯的「写出て形」归变形题区块；正题里同一批动词改考两种更复杂的形式：

```
在空格处填入合适的动词形式：
あの交差点を右に＿＿＿。          →  曲がってください
请在那个十字路口右转。   提示：转弯｜五段

把下面的中文翻译成日语：
在玄关脱鞋。                      →  玄関で靴を脱ぎます。
```

**题面完全不出现日语原词**：不说该用哪个动词，也不说该用哪种形式，只给中文和词类。得先从中文想出是哪个动词，再从句子判断该用ます形、て形还是ください。整句翻译连提示也没有——译对整句本身就证明这个动词会用。

V-004 的 100 个动词本身没有例句，这两种题的句子是从手册其他条目的例句里借来的（借用时会校验译文不含假名，避免把对话例句的第二行日语当成中文）。68 个动词能借到句子；借不到的那 32 个仍出读音题。

### 干扰项从哪来 | Where distractors come from

选择题的错误选项按以下优先级挑选，越靠前越有迷惑性：

1. **手册自己记录的易混淆项**——解析 `易混淆表达` 和 `Related` 小节，既认编号（`G-030`）也认名称（`～ように`）。因此 `ために` 的干扰项会是 `ように`，而不是随便一个无关语法。
2. 同词性
3. 同类别

变形题的干扰项是**同一个动词的错误变形**，按错用动词类型的规则生成，例如 `遊ぶ` 的て形会给出 `遊びて`（误用一段规则）、`開ける` 会给出 `開けって`（误用五段规则）、`飲む` 的可能形会给出 `飲まれる`（用了ない形词干）。这样考的是活用规则，而不是「哪个词眼熟」。

**ます形不再单独出题**——它是最先掌握的形式，出了也考不出东西。只有在某个动词推导不出更难的形式时才会退回到ます形。答案接受汉字或全假名两种写法（`話せる` 与 `はなせる` 都算对）。

### 题库外的词 | Words outside the item bank

例句里会出现没有单独收录的词（`鍵（かぎ）`、`砂糖（さとう）`、`風邪（かぜ）` 等），这是允许的——手册按 [AI_CONTENT_EXTRACTION_GUIDE.md](../AI_CONTENT_EXTRACTION_GUIDE.md) §5 只收录有长期价值的词，例句不受此限。

但**被考的对象必须是题库条目**，不能拿题库外的词的含义当考点：

| 题型 | 考的是什么 |
|------|-----------|
| 选择题（含义／读音） | 该条目本身的释义或读音 |
| 填空题 | 挖空处始终是该条目本身 |
| 变形题 | 该条目的活用形 |
| 改错题 | 该错误条目的正确说法 |
| 翻译题 | 提示中点名的那个条目 |

翻译题是唯一会连带要求写出题库外词汇的题型（整句作答）。这也是它判分宽松的原因：接近参考答案时显示「与参考答案不同，请自行判断」，并可手动标记为答对。

### 严重程度区分 | Error vs unnatural

`06-Mistakes.md` 用 `❌` 表示真正的错误，用 `△` 表示语法正确但不够自然。两者不会混为一谈：

- `❌` 条目 → 改错题（「请改成正确的说法」）
- `△` 条目 → 「下面哪种说法更自然？」

把 `△` 的句子判成「错误」会教错东西，因此生成器不这样做。

---

## 3. 复习机制 | Review logic

每个知识点维护一份学习状态（`docs/data/mastery.json`）：

```json
{
  "item_id": "G-020",
  "correct_count": 3,
  "wrong_count": 1,
  "streak": 2,
  "last_seen": "2026-08-17",
  "last_wrong": "2026-08-15",
  "mastery": 0.867
}
```

`mastery` 是平滑后的正确率，再按当前连续答对次数上调，因此**最近连续答对**比**很久以前答错**更有分量。选题规则：

- `mastery < 0.75` 视为尚未掌握；生成器每天**预留 2 个名额**给掌握度最低的知识点，保证错题一定会回来，而不是靠权重碰运气。
- 掌握度越低，普通名额里的权重越高。
- 最近 10 天出过的知识点降权；但**尚未掌握的降权很轻**，否则刚做错的题反而会被压下去。
- 已掌握的知识点隔 14 天后重新提权，进入间隔复习，不会永远消失。

同一个知识点再次出现时，会尽量换一个例句或换一种题型，考的是同一个知识而不是同一道题的记忆。

### 用你自己写错的句子出题 | Replaying your own errors

自由作答题（翻译、填空、改错、变形）答错时，网页应用会把**你实际写的那句话**一起上报。之后该知识点再出现时，会优先出成这样一道改错题：

```
这是你在 2026-08-15 写过的答案，请改成正确的说法：
❌ 話しすぎてください
想表达：请注意不要说得太多。
```

两条保护规则：

- **判为「接近」的答案不会被这样回放。** 自由作答判分是机械比对，接近参考答案的写法往往本身就是对的；把它当成错误回放，等于教错东西。这类知识点仍然会回来复习，但用普通题型。
- **只接受该句的参考答案**，不接受该知识点的其他例句——那些例句语法相同但意思不同。

选择题答错不会记录你选了哪个（错误选项本来就在题目里）。

> **注意**：如果仓库是公开的，`docs/data/mistakes.jsonl` 里会包含你写错的句子原文，任何人都能看到。介意的话可以把仓库设为私有（此时 GitHub Pages 需要付费方案，可改为本地运行）。

---

## 4. 日常使用 | Daily use

1. 打开网页应用（见下方部署说明），默认显示当天的练习。
2. 想多做几题就点对应板块下方的「再出 5 题」（见下）。
3. 做完后点击「提交答案」查看判分与解析；做了一半也可以先提交，空着的题不算错（见下）。
4. 点击「把错题记录到 GitHub」——会打开一个预填好的 Issue，直接提交即可。
5. GitHub Actions 会自动记录结果、关闭 Issue，并让错题进入明天的复习队列。
6. 回到应用即可——它会核对仓库里已入库的日期，把该天从待提交队列里自动清掉。

### 再出 5 题 | Adding more questions

正题和动词变形题下方**各有一个按钮**，点一次加一组 5 题，每块最多加两组（默认），因此一天最多 15 + 15 题。两块互不影响：只想多练变形就只点变形那个。

- **加练的题是当天一起生成的**，不是现场算出来的：网页是纯静态页面，背后没有生成器；而且同一天重新打开必须看到同样的题，否则做过的题会变。
- **一整天里没有任何一道题会重复**——加练题与正题、变形题互斥，例句也不重复。
- **加练题的错题照样上交**。可以做完 5 题先提交，再加一组接着做，然后再提交一次：判分只处理还没判过的新题，已判过的卡片不会被重新判分，也不会丢掉你勾过的「我的答案其实也对」。提交的记录始终是当天的**累计**结果，摄入端按日期替换，因此不会重复计数。
- **刷新页面也不会丢**。当天已经答过的题、已经加出来的题、以及勾过的「我的答案其实也对」，都存在本机（`localStorage` 的 `jp-day-state`，只保留最近 7 天），重新打开会原样恢复。想从头再做一遍点「再做一次」，记录随之清空。

### 做一半也可以提交 | Submitting part of a day

**空着的题不算错，也不会上报**——它是「没做」，不是「做错了」。所以随时可以按「提交答案」：

- 只判已经作答的题，空着的卡片保持原样，还能继续填；
- 得分的分母是**已作答的题数**（`得分：3 / 3`），下面会提示还剩几题；
- 填好之后再按一次提交，新答的题追加进当天的记录，先前判过的不会被重判；
- 一题都没答就按提交，什么也不会发生（按钮会提示一下）。

这样做的理由：把空题记成错，等于把一个根本没看过的知识点推进复习队列，那是假信号。跳过的题不留任何记录，之后照常按权重轮到它。
- 题多的时候，预填 Issue 的链接放不下你写错的句子原文，应用会只上报对错并在成绩下方说明；要保留原句可以用「复制结果 JSON」自己建 Issue。
- 用 `--extra-batches N` 调整备用组数，`0` 表示不出——此时按钮不会出现（更早生成的日期没有备用题，同样不显示按钮）。

翻译题和填空题的判分是机械比对，同一句话常常有多种正确写法。如果你的答案其实也对，可以勾选「我的答案其实也对，按答对记录」，结果会按答对提交。

**如果 GitHub 页面打不开或报错**（例如在应用内浏览器里没登录，被跳到登录页），结果不会丢：在你点「已提交，清除本机记录」之前它一直存在本机，下次打开应用会出现在顶部提示条里，重新提交即可。也可以点「复制结果 JSON」，自己新建一个带 `exercise-result` 标签的 Issue 粘贴进去。

**同一天不会被重复记录。** 应用会自动清除已入库的天；万一还是提交了两次，摄入端按日期替换而非追加，最后一次提交即为该天的记录。

---

## 5. 部署 | Deployment

网页应用是纯静态的，可以直接用 GitHub Pages 托管：

1. 仓库 **Settings → Pages**。
2. Source 选择 **Deploy from a branch**。
3. Branch 选择默认分支，目录选择 **/docs**。
4. 保存后访问 `https://<用户名>.github.io/japanese-handbook/`。

本地预览：

```bash
cd docs
python3 -m http.server 8000
# 打开 http://localhost:8000
```

### 装到手机上 | Install on a phone

应用是 PWA，可以像 App 一样装到主屏幕：

- **iPhone**：Safari 打开网址 → 分享 → 「添加到主屏幕」
- **Android**：Chrome 打开网址 → 菜单 → 「安装应用」／「添加到主屏幕」

装好后是独立图标、全屏运行，没有浏览器地址栏。

**可以离线做题。** Service worker 会缓存应用本身和当天的题目，地铁上没信号也能打开。首次访问时就会预先缓存最新一天，不需要先联网访问第二次。

离线完成的练习会**存在本机**，联网后页面顶部出现提示条，点一下就能提交：

```
有 1 天的练习结果还没上报，点下面的按钮提交：
[ 提交 2026-08-16 ]  [ 复制 ]  [ 全部清除 ]
```

点「提交」只是打开 GitHub 页面，结果**仍然留在队列里**；等 workflow 入库后回到应用，该天会自动从队列消失。这样 GitHub 报错或中途放弃都不会丢数据，提交成功也不需要手动清理。变成「已提交 …，清除」的那个按钮是离线时的手动退路。

同一天重复作答只保留最后一次。没提交就关掉页面也不会丢——下次打开还在。

相关文件：`manifest.json`（应用名称、图标、启动方式）、`sw.js`（缓存策略）、`icon-192.png` / `icon-512.png` / `icon-180.png`（图标）。

缓存策略分两种，因为两类文件的需求相反：

| 内容 | 策略 | 原因 |
|------|------|------|
| 应用本身（html/js/css/图标） | 缓存优先，后台更新 | 很少变，启动要快 |
| 题目数据（`data/*.json`） | 网络优先，失败回落缓存 | 否则缓存的 `index.json` 会挡住当天新生成的题 |

改动 `sw.js` 或应用文件后，把文件里的 `VERSION` 加一（`v1` → `v2`），旧缓存会在下次激活时清掉。

---

## 6. 自动化 | Automation

| 工作流 | 触发条件 | 作用 |
|--------|----------|------|
| `.github/workflows/exercise-daily.yml` | 每天 UTC 午夜（`0 0 * * *`），也可手动触发 | 生成当天练习并提交到仓库 |
| `.github/workflows/exercise-ingest-results.yml` | 带 `exercise-result` 标签的 Issue | 记录答题结果、更新错题统计、关闭 Issue |

首次使用前，请在仓库里创建 `exercise-result` 标签（网页应用生成的 Issue 链接会自动带上该标签，但标签本身需要存在）。

结果 Issue 只接受**仓库所有者**提交，避免他人污染学习记录。

### 关于每日生成的时间 | About the daily schedule

练习在**每天 UTC 午夜**生成，对应英国时间冬令时 00:00、夏令时 01:00。

GitHub Actions 的 cron 按 UTC 计算且**不跟随夏令时**，所以锚定在 UTC 反而最简单：全年只需要一个表达式 `0 0 * * *`，不需要任何季节性修正。

练习日期直接用运行机的 UTC 日期（`date -u +%F`）。在 UTC 午夜这一刻，英国的日历日期与 UTC 相同（冬令时 00:00、夏令时 01:00，都还在同一天），因此不需要做时区换算。

手动触发（workflow_dispatch）可以指定任意日期，任何时间都会执行。

---

## 7. 手动运行 | Manual commands

```bash
# 生成今天的练习
python3 exercise-generator/generate_exercises.py

# 生成指定日期，并覆盖已有文件
python3 exercise-generator/generate_exercises.py --date 2026-08-20 --force

# 调整变形题数量（0 表示不出变形题）
python3 exercise-generator/generate_exercises.py --drills 10

# 调整备用题组数（0 表示不出备用题，应用里就不会出现「再出题」按钮）
python3 exercise-generator/generate_exercises.py --extra-batches 3

# 查看解析出的题库（调试用）
python3 exercise-generator/itembank.py > /tmp/bank.json

# 手动录入一次答题结果
python3 exercise-generator/ingest_mistakes.py --body-file result.txt

# 让 LLM 讲解错题（见下节；--dry-run 只打印提示词，不联网）
python3 exercise-generator/explain_mistakes.py --dry-run --limit 3
python3 exercise-generator/explain_mistakes.py --date 2026-08-23
```

### 用 LLM 讲解错题 | LLM explanations（可选，需 API key）

应用只能说「正确答案是 X」，说不出**错在哪条规律上**。`explain_mistakes.py` 补这一半：把手册条目原文、当天的题目、你写的答案和参考答案交给 Claude，要一段两三句的中文讲解，写回 `mistakes.jsonl` 的 `analysis` 字段。

```bash
pip install anthropic
export ANTHROPIC_API_KEY=...
python3 exercise-generator/explain_mistakes.py            # 讲解所有还没讲过的
python3 exercise-generator/explain_mistakes.py --dry-run  # 只打印提示词，不联网、不需要 key
```

喂给模型的材料全部来自手册，不是它自己的记忆：

- **条目原文**（`构成`、`注意事项` 都在里面，题库只留了释义和例句）；
- **手册标记为容易混淆的条目**——语法类取 `Related` 段；词汇没有 `Related`，改由「常见搭配／区别」列里写明的对照（`自他动词对：…`、`与「話す」同音不同字`）以及**同假名**、**同汉字词干且一自一他**推出，所以 `始まる／始める` 这类词对即使手册没写明也能配上；
- **已经学过的语法／助词／固定表达清单**，讲解可以直接引用（「这是 G-019 的反面」比一个陌生的语法名管用）。

除讲解外还回一个判断 `learner_answer_ok`：你写的其实也成立——判分是机械比对，接近参考答案的写法常常本身就对。

参考答案若用到了条目里没写的语法，讲解会**照样讲清楚**，不会停下来说手册没收录。

几条边界：

- **不进每日生成。** 生成必须保持确定性——同一天重跑结果一致是可复现和可调试的基础。
- **只写 `docs/data/`，不碰 `handbook/`。** LLM 产出的是派生数据。
- **只讲自由作答题。** 选择题没有记录你选了哪个（错误选项本来就在题目里），无从讲起。
- 同一天再次提交时 `ingest` 会替换该天的记录，讲解随之丢失，重跑一次即可。
- 成本可忽略：每次约 1900 输入 token，一天 0–3 道自由作答错题。

---

## 8. 数据文件 | Data files

| 文件 | 内容 |
|------|------|
| `docs/data/exercises/YYYY-MM-DD.json` | 每天的 5 道正题与 5 道变形题，外加两块各 2 组备用题（含答案与解析） |
| `docs/data/index.json` | 已生成的日期列表 |
| `docs/data/history.jsonl` | 每天出过哪些知识点（`item_ids` 正题、`drill_item_ids` 变形题、`extra_*` 备用题） |
| `docs/data/mistakes.jsonl` | 错题记录 |
| `docs/data/attempts.jsonl` | 全部答题记录（对与错） |
| `docs/data/stats.json` | 错题统计汇总 |
| `docs/data/submitted.json` | 已入库的日期清单，应用据此自动清理待提交队列 |
| `docs/data/mastery.json` | 每个知识点的掌握度状态 |

应用文件：`index.html`、`app.js`、`style.css`、`manifest.json`、`sw.js`、`icon-*.png`。

---

## 9. 扩充题库 | Extending the item bank

题库直接来自手册，因此**给手册增加内容就等于给练习增加题目**，不需要改代码：

- 语法、助词、固定表达：新增带固定锚点的条目，并至少写一个「> 日语例句 / > 中文翻译」形式的例句。
- 词汇：在 `05-Vocabulary.md` 的对应词性表格中增加一行。
- 易错点：在 `06-Mistakes.md` 中增加条目，用 `❌` 标注错误句、`✅` 标注正确句，会自动变成改错题。
- 动词：在 `02-Verbs.md` 的 V-004 速查表中增加一行，会自动进入变形题。

写 `易混淆表达` 或 `Related` 小节同样有用——生成器用它来挑选有迷惑性的干扰项。

带例句的条目才能出填空题和翻译题；改错题和变形题使用各自章节的结构化数据，不需要例句。
