## v0.13.1 — 2026-08-22

### Changed
- `PROJECT_SPEC.md` §14.1 新增 **「衍生改动跟随它所属的能力」**：一次改动如果是先前某个次版本号引入的能力在使用中衍生出来的——暴露出的问题、不得不补齐的环节、随之要调整的显示——一律计为修订号，因为那次使用方式的变化在引入时已经计过一次
- 给出两步判断顺序：先问「能不能脱离前一个能力独立成立」，不能就是修订号；能独立的再分「修掉一个错误行为」（修订号）与「多出一种新做法」（次版本号）
- 以 v0.12.0「再出 5 题」及其两个衍生版本（v0.12.1、v0.13.0）为例写进条文

### Notes
- 按新条文，**v0.13.0 应为修订号**：分次提交是加题衍生出来的需求，没有「一天可能做不完 30 题」就不会有它。已发布的号不重编，v0.13.0 保持原样，v0.13.0 的 Notes 里已注明不要当判例
- 此前定这两个版本号时，v0.12.0 依据明确，v0.13.0 属边界情况而没有在合并前说明——这条规则就是为了让这类判断以后不必再临时权衡
- 按 §14.1 计为**修订号**：文档更正，使用方式不变

## v0.13.0 — 2026-08-22

### Changed
- **空题不再按错判，因此做了一半也可以提交**。空着的题是「没做」而不是「做错了」：把它记成错，等于把一个根本没看过的知识点推进复习队列，那是假信号
  - 「提交答案」只判**已经作答**的题；空着的卡片保持原样，输入框不禁用，还能继续填
  - 得分的分母是已作答的题数（`得分：3 / 3`），下面提示还剩几题；两块各自按自己作答的数量计分，一块都没碰就不显示那一段，而不是显示 `0 / 0`
  - 填好之后再提交一次，新答的题追加进当天的记录，先前判过的不会被重判，勾过的「我的答案其实也对」也不会丢
  - 一题都没答就按提交，什么也不会发生——按钮短暂显示「还没有作答的题」，不会打开空的成绩面板，更不会上报一天的空白
- 跳过的题不留任何记录：既不算对也不算错，掌握度不受影响，之后照常按权重轮到它
- `sw.js` 的 `VERSION` 升到 `v11`

### Notes
- 与 v0.12.1 的当天存档相容：做一半提交后刷新，已判的题、剩下的空题、以及「还有 N 题空着」的提示都原样恢复，空题依然可填
- 浏览器端 5 组场景通过：空提交无操作、10 题里答 3 题提交、余下 7 题续答后累计为 10、做一半后刷新、只答变形题时只显示变形那一段。v0.12.0／v0.12.1 的三套既有测试重跑全部通过
- 版本号按 §14.1 计为**次版本号**（理由：可以分次提交一天的练习，是使用方式的变化）。后来在 v0.13.1 给 §14.1 补了「衍生改动跟随它所属的能力」一条，按那一条这里**应为修订号**——分次提交是 v0.12.0「再出 5 题」衍生出来的需求，脱离它不成立。已发布的号不重编，此处保持原样，但不要拿它当以后的判例

## v0.12.1 — 2026-08-22

### Fixed
- **刷新页面后再提交，会把已经答对的题报成答错**。判分只处理没判过的卡片（v0.12.0 为了「提交后还能加题」而改成增量），但刷新会把所有卡片重新渲染成空白、`graded` 清零——于是下一次「提交答案」把这些空白判成错，而摄入端按日期替换，正确记录就被错误记录覆盖了，那些知识点还会被推进复习队列，正好推反
  - 复现：10 题全答对提交 → 刷新 → 加 5 题只答新的 → 提交，结果是 `得分：5/10　·　变形：0/5`，第一轮答对的 10 题全部变成答错
  - 修法：当天的作答状态存进 `localStorage` 的 `jp-day-state`（已答的题与答案、已加出来的组数、勾过的「我的答案其实也对」），重新打开时原样恢复。只留最近 7 天——这是便利，不是记录，真正的记录在仓库里
  - 顺带修好一个更早就存在的毛病：提交前刷新会丢掉当天已经写好的答案
- 「再做一次」现在会清掉当天的存档，否则页面会带着已判分的卡片直接回来

### Changed
- `sw.js` 的 `VERSION` 升到 `v10`

### Notes
- 恢复时不直接采信存下来的对错，而是拿存下来的答案重新跑一遍判分，再按需要补上勾选状态——这样卡片连「我的答案其实也对」那个复选框都跟刷新前一模一样（它只在机器判错时出现）
- 浏览器端 5 组场景全部通过：未判分时加的题也能恢复、勾选状态跨刷新保留、刷新后继续加题仍是累计上报、「再做一次」清空、更早生成的日期（没有备用题）照常渲染
- 按 §14.1 计为**修订号**：修 bug，使用方式不变

## v0.12.0 — 2026-08-22

### Added
- **「再出 5 题」**：正题与动词变形题下方各有一个按钮，点一次加一组 5 题，每块默认最多加两组，一天最多 15 + 15 题。两块互不影响，只想多练变形就只点变形那个
- 加练的题**与当天的题一起生成**，写在 `YYYY-MM-DD.json` 的 `extra_exercises` / `extra_drills` 里。网页是纯静态页面、背后没有生成器，而且同一天重新打开必须看到同样的题，所以不能现场出题
- `--extra-batches N` 控制备用组数（默认 2，`0` 表示不出，此时按钮不出现）

### Changed
- **提交格式改为编号清单**，不再是每题一行：
  ```json
  {"date":"2026-08-22","correct":["G-002"],"missed":["W-N163"],"given":{"W-N163":"雪に転びました"}}
  ```
  30 道题时旧格式把 `item_id`、`type`、`correct` 各重复 30 遍，预填 Issue 的链接经登录跳转包一层后逼近 7800 字符——正是 2026-08-17 那次 500 的成因。题型和参考答案都在当天的练习文件里，摄入端照 `expected` 的老办法自己读。实测 30 题满负荷时链接由 5226 降到 826 字符
- 摄入端同时接受新旧两种格式：手机待提交队列里可能还存着旧格式，仓库里已有的 Issue 也是旧格式
- **判分改为增量**：只判还没判过的题。因此可以先做 5 题提交，再加一组接着做，再提交一次——已判过的卡片不会被重新判分，勾过的「我的答案其实也对」也不会丢。提交的始终是当天累计结果，摄入端按日期替换，不会重复计数
- `main()` 拆出 `build_main_block()` 与 `build_drill_block()`，备用题走的是与正题完全相同的选题、配额与题型平衡逻辑，只是候选池里去掉了当天已出过的条目
- 每组备用题各自预留自己的复习名额，因此加题是**增加**复习题而不是稀释它们——`NEXT_STEPS.md` 里记的 `REVIEW_SLOTS` 联动问题按这个方式解决
- `sw.js` 的 `VERSION` 升到 `v9`

### Fixed
- **同一天生成两次可能得到不同的干扰项顺序**。`_distractors` 直接遍历 `confusion` 里的 `set`，而 Python 的字符串哈希每个进程都不同，所以易混淆词条的初始顺序随进程变化；后面的 `rng.shuffle` 只是打乱收到的顺序，救不回来。改成 `sorted()`。这是既有缺陷，条目要有两个以上易混淆词才会显形，题量翻三倍后才稳定复现

### Notes
- 一天之内没有任何一道题会重复：备用题与正题、变形题互斥，例句也互斥
- 题目多到链接放不下你写错的原句时，应用只上报对错，并在成绩下方说明；要保留原句可以用「复制结果 JSON」自建 Issue
- 每天的练习文件由约 7 KB 增至约 22 KB
- 更早生成的日期没有备用题，按钮不显示，其余功能照旧
- 40 天 × 2 组哈希种子的生成回归 0 失败、结果逐字节一致；浏览器端跑通了「加题 → 判分 → 再加题 → 再判分 → 提交」全流程，25 张卡片各有且只有一个判分块
- 按 §14.1 计为**次版本号**：新增了练习区块的加题能力，使用方式随之改变

## v0.11.4 — 2026-08-21

### Added
- `handbook/01-Grammar.md` 新增三条语法：**G-040 ～た方がいい**（建议；否定用 `～ない方がいい`）、**G-041 动词普通形＋名词**（动词修饰名词的一般规则）、**G-042 ～まま**（保持状态不变）
- `handbook/04-Expressions.md` 新增三条表达：**E-017 しょうがない**、**E-018 このまま**、**E-019 もう少しだけ**
- `handbook/05-Vocabulary.md` 新增 10 条词汇：名词 `消しゴム`、`数`、`右側`、`左側`、`幽霊`、`芸術`（W-N178 – W-N183），动词 `数える`、`巡る`、`離す`、`降りる`（W-V067 – W-V070）
- `handbook/06-Mistakes.md` 新增三条易错：**M-012 話す vs 離す**、**M-013 毎月に**、**M-014 おります 的两种来源**

### Changed
- **G-017 引用助词「と」＋言う** 补上口语的 `って`：构成加一行 `普通形 + って言う（口语）`，例句加两句，注意事项说明 `～と言いました` 与 `～って言いました` 的语体差别，并点明这里的 `って` 是引用标记而非动词
- `handbook/99-Index.md` 收录以上 19 个新编号

### Notes
- 内容来自一份按 v0.11.2 生成的更新包，落库时按当前仓库状态重新核对过：
  - **`普通`、`不思議` 未新增**——已有 [W-NA007](handbook/05-Vocabulary.md#W-NA007)、[W-NA009](handbook/05-Vocabulary.md#W-NA009)
  - **`紹介する` 未新增**——[W-N140 紹介](handbook/05-Vocabulary.md#W-N140) 的常见搭配里已经写了 `友達を紹介する`；仓库里 10 个「名词＋する」动词条目没有一个同时收了对应名词，不为一个概念开两条
  - **`降りる` 保留**——虽然 V-004 速查表第 023 行有它，但那是变形速查表，`W-V` 是带词义与搭配的词条；`入る`、`始まる`、`払う` 等 7 个动词本来就同时存在于两处
- 按 §8.1 逐条核对了新动词的变形：`数える`（一段）、`巡る`、`離す`（五段）、`降りる`（一段）推导全部符合规则，**无需登记进 V-009**。`降りる` 的 `り＋る` 形似五段，且ます形与 `降（ふ）る` 撞成同一个 `降ります`，只有て形能分开（`降りて` ／ `降って`），这一点写进了词条的「常见搭配」栏
- 更新包里的 M-014 原本用 `### Potential confusion` 标题、正确句也没有 `✅` 标记，`itembank.py` 会整条跳过；改写成仓库的 Wrong／Correct／Why 结构后才真正进题库
- 更新包所有例句缺行尾两个空格（软换行），日语与中文会挤在一行；已补齐。`### Related` 的裸编号也按 §4.5 改成了可点链接
- 题库由 523 条增至 542 条。60 天生成回归 0 失败，同日重跑结果一致
- 按 §14.1 计为**修订号**：只是增删内容，使用方式不变

## v0.11.3 — 2026-08-21

### Fixed
- **同一天的结果被重复提交**：v0.8.4 起，待提交队列会一直保留到手动点「已提交，清除本机记录」为止，而这一步很容易漏——于是提示条第二天照旧显示，结果又提交一次。`2026-08-19` 因此进了 **4 次**（30 行，应为 10），`2026-08-20` 进了 2 次
- 修在两端：
  - **应用自动核对**：`ingest` 现在维护 `docs/data/submitted.json`（已入库的日期清单）；应用启动、恢复联网、以及从 GitHub 标签页切回来时都会核对一次，已入库的天自动从队列里清掉。手动按钮保留为离线时的退路
  - **摄入按日期替换而非追加**：同一天再次提交时先删掉该天的旧记录。重复提交因此变成幂等操作，不会再累积

### Changed
- 清理已产生的重复：`attempts.jsonl` 80 → 50 行，`mistakes.jsonl` 30 → 17 行，每个日期只保留最后一次提交（issue 编号最大的那次）
- `sw.js` 的 `VERSION` 升到 `v8`；结果页说明文案相应改写

### Notes
- 重复记录会歪曲掌握度：`W-N163` 原本被记了 5 次错，清理后是 2 次；`W-V008` 由 5 次降为 1 次。清理后复习队列的优先级才是准确的
- `docs/data/` 属派生数据（`PROJECT_SPEC.md` §3），重复行是错误记录而非学习历史，清理不损失任何真实作答
- 按 §14.1 计为**修订号**：修 bug，使用方式不变

## v0.11.2 — 2026-08-18

### Added
- `handbook/02-Verbs.md` 新增 **V-009 不规则变形一览**：按 V-002 – V-008 的类型规则推不出正确形式的动词集中登记于此。格式 `形式＝值`，多个用 `／` 分隔，`—` 表示该形式现代日语不使用、不作为练习目标。首批 13 条：`行く`、`問う`、`ある`、`する`、`来る`、`いる`、`開く`、`入る`、`違う`、`分かる`、`できる`、`見える`、`聞こえる`
- `AI_CONTENT_EXTRACTION_GUIDE.md` 新增 **§8.1 Verbs: check the conjugation before recording**：收录动词前先确认词类（提醒 `帰る`、`走る`、`切る`、`要る`、`知る` 这类形似一段的五段动词），再逐项核对推导结果，不符合规则的补进 V-009；符合的不必登记。自检清单相应加一项
- `check_id_pattern.py` 增加 V-009 校验：表解析为空即判失败，登记的动词若在手册里找不到对应条目则告警——否则表里的错字会静默失效，练习照旧给出错误答案

### Changed
- 生成器不再硬编码任何不规则变形。原先 `IRREGULAR_FORMS`、`IRREGULAR_TE`、`NO_POTENTIAL` 三处写着 `行く→行って`、`する→できる` 这类日语知识，违反 `PROJECT_SPEC.md` §3（手册是唯一知识来源，练习应用只读）。现在全部改由 `load_irregular_forms()` 从 V-009 读取

### Notes
- **62 条 W-V 动词已逐条核对**：36 个五段的て形推导全部正确（`焼く`、`泣く`、`履く`、`引く`、`咲く`、`気づく` 等 `く` 结尾的均为规则变化，不存在 `行く` 型；`う` 结尾的也没有 `問う`／`請う` 型），16 个形似一段的确认都是真一段，10 个サ变均为「名词＋する」——**没有不规则的**
- 自动词不出可能形这一条仍由词条上的 `自动词` 标注自动生效，不逐个登记进 V-009；V-009 只记标注覆盖不到的情形
- V-009 与 V-001、V-004 一样是参考表，不进题库、不出题
- 按 §14.1 计为**修订号**：只是把已有知识挪进手册并补上流程，使用方式不变

## v0.11.1 — 2026-08-18

### Changed
- 变形题的动词池从 **100 扩到 162**：除 V-004 表外，`05-Vocabulary.md` 里 62 条带词类的 `W-V` 动词也可出题。60 天回归里涉及的不同动词由 73 个增加到 129 个
- `derive_form` 现在能按词类从辞书形推出ます形与て形（此前只能读 V-004 表里写死的两列，所以 `W-V` 条目只能推出 2 种形式、凑不够一道转换题）
- 新增 `IRREGULAR_TE`：`行く` 一类的特例在 V-004 表里有，但 `W-V` 没有表可依，单列一份兜底清单

### Notes
- 推导规则用 V-004 的 100 个动词逐条验证：ます形与て形共 200 项，除 `行く→行って`（表里写死，优先采用）外**完全吻合**
- **固定搭配与复合动词整条排除**：`気をつける`、`手を洗う`、`お腹が空く`、`付いてくる`——「「手を洗う」→ 写出た形」考的是词组不是形式
- **自动词不出可能形**：`始まれる`、`咲ける`、`壊れられる` 不自然；`付ける`、`並べる`、`入れる` 更会撞上自己的他动词对，等于教错一组对应关系。手册的自他标注正好可以直接用
- V-004 没有自他标注，另按词单列：`開く`、`入る`、`違う`、`分かる`、`できる`、`見える`、`聞こえる` 不出可能形。`する→できる` 保留——那是标准的补充形式
- 受影响的动词仍可练其余四五种形式，162 个动词全部至少能出两种形式
- 按 `PROJECT_SPEC.md` §14.1，本次只扩充内容与修正推导、使用方式不变，计为**修订号**

## v0.11.0 — 2026-08-18

### Added
- **每天另加 5 道动词变形题**：正题之后独立成块，没有例句也没有上下文——给出动词的某一种形式，写出另一种（`「歩きます」（ます形）→ 写出可能形：`）
  - 给出的形式在辞书形／ます形／て形之间轮换（权重 5：3：2），目标形式取辞书形、て形、た形、ない形、可能形之一；ます形只在推不出别的形式时才作为目标
  - 答案接受汉字或全假名（`歩ける` 与 `あるける` 都算对）
  - 解析里给出该动词的完整变形表，答完即可对照
  - 生成器新增 `--drills N`，默认 5，`--drills 0` 关闭
- 应用新增「动词变形练习」区块：独立标题、独立编号（变形 1–5）、左边框用主题色区分；计分与正题分开显示（`得分：4 / 5　·　变形：3 / 5`）
- `drill_problems()`：变形题与正题一样有出厂检查——数量、答案非空、`accepted` 非空、答案不得与题面给出的形式相同、同一天不得重复同一个动词

### Changed
- **正题里的动词改考更难的形式**：新增两种题型，都以 V-004 动词为考点
  - `verb_form_choice`：句子里挖空，**题面完全不出现日语原词**——只给中文句子、动词的中文释义和词类（`提示：转弯｜五段`），自己想出是哪个动词、再判断该用哪种形式（`あの交差点を右に＿＿＿。` → `曲がってください`）
  - `verb_translation`：整句中译日，**不给任何提示**。点名日语动词等于交出答案；只给中文释义又会在动词藏在句型里时误导（`ようになりました` 里的 `なる` 不是学习者理解的「变成」）。译对整句本身就证明这个动词会用
  - V-004 的 100 个动词没有自带例句，句子从手册其他条目的例句里借用；借用时校验译文不含假名，否则对话例句（`A：…／B：…`）的第二行日语会被当成中文题干
  - 68 个动词能借到句子（平均 9.3 句）；借不到的 32 个仍出读音题
  - 动词条目不参与题型平衡：平衡会不断把最简单的读音题提到前面，而写形式本来就归变形题区块管了
- **正题里不再出单纯的变形题**：形式转换归新区块管。`build_exercise` 新增 `exclude` 参数——原先未被选中的格式仍会在兜底遍历里复活，`なる`、`ある` 这类没有读音题可出的动词就又变回了变形题
- 选题循环增加补位：某个知识点造不出任何题目时从备选里补一个，当天不会因此少于 5 题（此前会静默变少并被质量检查判死）
- 变形题的选词走独立的历史（`history.jsonl` 新增 `drill_item_ids`），因此昨天练过的动词今天不会再练，同时不影响它出现在正题里
- 同一个动词不会在同一天既出现在正题、又出现在变形题
- `ingest_mistakes.py` 回查正确答案时一并读取当天的 `drills`，变形题答错也能记录 `expected`

### Notes
- `PROJECT_SPEC.md` §14 补写 **14.1 该改哪一位**：默认只改最后一位，只有使用方式变化时才动次版本号；判断标准是使用方式而非改动量（加两百条词汇仍是修订号，加一个每天多做 5 题的区块是次版本号）。本版本按此规则计为次版本号
- 薄弱动词的名额有两天冷却期：动词池只有 100 个，唯一有错题记录的 `VF-063 習う` 原本会连续 29 天霸占名额；冷却后改为每三天左右回来一次，仍由掌握度驱动
- 冷却期内不再兜底强塞薄弱动词——名额让给加权抽样即可，抽样本身已经偏向薄弱项
- 变形题结果与正题一起提交，同样进入掌握度与复习队列
- `--drills 0` 时变形题自动回到正题的题型池，行为与本版本之前一致
- 同一天重复生成结果仍然完全一致（确定性未被破坏）

## v0.10.0 — 2026-08-17

### Added
- 手写笔记第二批，共 **158 条**词汇与 4 个知识点：
  - 名词 `W-N115` – `W-N177`（63 条）：学习创作、抽象概念、场所位置、饮食、人物职业、事物属性、商业购物、家务、风景旅游、天气健康、家庭亲戚、方位、生活杂项、婚礼与事故
  - 动词 `W-V030` – `W-V066`（37 条）：情绪心理、处理丢弃、确认查找、交往联络、身体状态、动作行为、移动与日常
  - い形容词 `W-I012` – `W-I041`（30 条）
  - な形容词 `W-NA009` – `W-NA022`（14 条）
  - 副词 `W-ADV021` – `W-ADV033`（13 条，含连体词「ほかの」，已在该条注明词性差异）
  - 接续词 `W-CON001`（1 条）：`そして`——该小节此前为空，本次建表
- `G-038 可能形＋名词（修饰名词）`：用可能形作定语，注明对象用「が」不用「を」
- `G-039 ～やすい／～にくい`：注明整体按い形容词活用，与可能形否定的区别
- `E-016 おつかれさまでした`：区分 `おつかれさまでした`／`おつかれさま` 的语体，并提示不可对上级说 `ご苦労さま`
- `P-012 だけ`：限定用法，注明 `が`／`を` 通常被 `だけ` 取代

### Changed
- 「男の人」「女の人」相邻收录，并在两条中说明与 [W-N013 男](handbook/05-Vocabulary.md#W-N013)、[W-N014 男性](handbook/05-Vocabulary.md#W-N014) 的语域差异（口语／简短直接／正式书面）
- `generate_exercises.py`：填空题的空格向右扩展时不再吞掉い形容词后面的 `です`（`天気が悪いです` 曾被挖成 `悪いで`）

### Fixed
- `P-010 から／まで` 与 `E-010 から／ので／だから／そのため` 补上一句总述：这两条的正文全是列表项或表格，解析器逐行跳过后取不到定义句，`meaning_zh` 为空字符串，导致解析文案出现「から／まで：」这样的空冒号，两条也无法出「……的意思是？」题。补写后两处均恢复正常，全库 523 条再无空释义

### Validation
- 与仓库现有全部条目逐条去重。笔记方已标出 13 处重复（おとな／おもいで／きもち／だいじ／ひどい／ちがう／いれます／つけます／はらいます／おくります／ころびます／はいります／ながら）与 2 处笔记内部重复（にがて、さいあく）
- 本次另发现并排除 **23 处**笔记方未标出的重复：
  - 名词 3：季節（W-N054）、値段（W-N051）、熱（W-N078）
  - い形容词 2：忙しい（W-I009）、苦い（W-I010）
  - 动词 18：探す・焼く・注文する・遅れる（W-V），忘れる・手伝う・疲れる・脱ぐ・借りる・急ぐ・座る・終わる・着く・使う・出かける・走る・歩く・泳ぐ（02-Verbs.md V-004 表）
  - 其中 できる、なる 是按**读音**比对才发现的（笔记写作「出来る」「成る」，V-004 表用假名），已计入上述 18 条
- 笔记里的「はかります＝称重」原指 `量る`（称量重量）；初稿误按「測る＝测量」收录，例句也写成了「体温を測りました」。已改为 `量る`，例句改为「荷物（にもつ）の重（おも）さを量（はか）ります。」，并注明同音异写的分工：重量用 `量る`，长度・时间・温度用 `測る`
- 笔记里的「なおります＝恢复」原指 `治る`，已收录于 `W-V026`，不是新词；初稿误按「直る＝修好」新增了一条，已删除并把后续动词编号顺移（该批编号尚未合并发布、零答题记录、无交叉引用）。`W-V026 治る` 的释义相应补为「痊愈；恢复」
- `泊まる` 与 V-004 表的 `止まる` 为同音异义词，已收录并注明区别
- 编号全部按各前缀当前最大值动态分配，无预设数字；`check_id_pattern.py` 通过（523 条），索引 340 条与词条一一对应、顺序一致，无死链
- 连续生成 60 天共 300 题：57 种不同填空，无空答案、无重复选项、无残缺词

## v0.9.0 — 2026-08-17

### Added
- 手写笔记第一批，共 **49 条**词汇：
  - 名词 `W-N032` – `W-N041`、`W-N087` – `W-N114`（38 条）：洗漱与医药用品、动物、身体部位、购物与商品、饮食、家庭称谓、时间与地点，以及易错读音 `四時（よじ）`、`九時（くじ）`
  - な形容词 `W-NA007` – `W-NA008`（2 条）：普通、順調
  - 副词 `W-ADV012` – `W-ADV020`（9 条）：ABAB 型拟态词
- **拟态词（ABAB 型）汇总**小节：归纳「AB 音节重复两次」的构词规律，把 11 个 ABAB 型词集中对照，不另行编号
- **附录：数字与时间读法**：时刻、分钟、月份、日期、天数、个数、～匹、～位共 8 个表，每表配 furigana 例句。附录为查阅用参考，不编号也不进入题库

### Changed
- `W-N016 理由` 释义由「理由；原因」精简为「理由；辩解依据」，与新增的 `W-N112 原因` 分工，两条互相注明区别

### Notes
- 名词只有 `W-N032` – `W-N041` 十个空号可用（v0.8.5 删除的十条留下，零答题记录），为不拆散主题，用**洗漱与医药用品（8）＋动物（2）**正好填满，其余主题整组接在 `W-N087` 之后
- 笔记原稿把「薬局」列为新词，实际已收录于 `W-N079`，本次未重复添加
- 笔记原稿「天数（～日間）」一节的 `いっかかん`／`にかかん`／`ごにちかん`／`ろくにちかん` 非通行读法，附录按标准读法记为 `ふつかかん`／`いつかかん`／`むいかかん`，并说明「一天」一般直接说「一日（いちにち）」

## v0.8.5 — 2026-08-17

### Removed
- 删除 `W-N032` – `W-N041` 十条名词（学生、先生、名前、友達、会社員、国、今日、明日、毎朝、時間）：已经记住，留在题库里只会占用每天 5 题的名额。词条与 `99-Index.md` 索引一并删除
- 词汇条目 143 → 133，题库 322 → 312

### Changed
- W-I008 词头 `美味しい` → `おいしい`，说明改为「反义是「まずい」；『美味しい』也是常见的汉字表记」；`99-Index.md` 索引词头同步
- W-N033 先生 的说明收紧为「称呼教师时常用，医生、律师等部分专业人士也可用作敬称」（该条随后在本版本中删除）
- 例句中的 `美味（おい）しい` 一律改为假名：`05-Vocabulary.md` W-I008／W-ADV004，`01-Grammar.md` 三处

### Notes
- 这十个编号在 `docs/data/attempts.jsonl`、`mistakes.jsonl` 与全部历史题目中均为零记录，删除不影响任何学习数据，编号本身也因此可以安全地留给以后的新词使用
- 这是一次性的内容整理，`PROJECT_SPEC.md` §4 第 7 条的编号永久性规则未作修改：**已产生答题记录的编号仍然不可回收**
- `MIGRATION_MAP.md` 保持原样，它记录的是 v0.8.0 迁移当时的状态

## v0.8.4 — 2026-08-17

### Fixed
- 主按钮「把错题记录到 GitHub」仍会在点击瞬间删除本机记录：v0.8.3 只修了待提交列表里的链接，漏了这一处，因此 2026-08-17 那天的作答在 GitHub 报 500 时就已经丢失
- 现在点击只打开 GitHub 页面；结果保留到用户点新增的「已提交，清除本机记录」按钮为止。GitHub 报错或中途放弃时，下次打开应用会在顶部提示条里重新出现

### Notes
- 反过来，提交成功后需要手动点清除，否则同一天会一直留在提示条里；重复提交会在 `attempts.jsonl` 留下两份记录

## v0.8.3 — 2026-08-17

### Fixed
- 从手机应用提交结果时 GitHub 返回 500：预填 Issue 的链接过长（未登录时还要经由 `/login?return_to=`，长度再翻一倍）。最长的一段是每题都附带的正确答案，而它本来就在仓库里
- **结果不再随点击丢弃**：原先点「提交」立刻把该天从本机队列里删掉，GitHub 报错时那天的作答就没了。现在点击只是打开页面，队列保留，确认提交成功后再手动清除
- 待提交的每一天旁边补一个「复制」按钮，GitHub 打不开时可以自己新建 Issue 粘贴

### Changed
- 提交内容不再包含 `expected`（正确答案），JSON 也不再缩进：最坏情况下链接从 2300 字符降到 1366 字符
- `ingest_mistakes.py` 改为按日期从 `docs/data/exercises/<date>.json` 回查正确答案；旧格式（自带 `expected`）仍然照常接受

## v0.8.2 — 2026-08-17

### Changed
- **变形题不再考ます形**：改考て形／た形／ない形／可能形。た形、ない形、可能形按 V-005 ～ V-007 的类型规则从辞书形推导（100 个动词全部可推导四种形式，逐一核对无误）
- 新增对应的错误变形干扰项：可能形给出 `飲まれる`（ない形词干）、`食べれる`（ら抜き）；ない形给出 `飲みない`（ます形词干）、`食べらない`（当作五段）；サ变、カ变同样有规则化的错项
- 变形题接受全假名答案（`話せる` 与 `はなせる` 都算对），手机上不必切换汉字输入
- **填空题挖掉整个词**：原先只挖句型部分，`毎日練習し＿＿＿` 只考两个假名；现在连同活用与词干一起挖掉，成为 `毎日＿＿＿上手になります。`（答案 `練習したら`）
- 空格变大的题目附中文提示，否则等于纯猜；词汇填空本来就给了整句中文，不再重复提示

### Notes
- `ある` 不再出可能形题：`有り得る` 是另一个词，不是活用形
- 空格向右只按活用词尾表扩展，不再吞掉相邻的词（`ように＋ま` 曾把 `まとめます` 截断）；向左扩展只对语法句型生效，名词保持自身边界（`二人が行ったお城` 曾被整体挖掉）
- 助词 `に` 不计入词干连接（`上司に説明します` 的 `に` 留在空格外），例外只有接 `ます` 的情形（`死にました`）
- 以 120 天连续生成回归验证：600 题、95 种不同填空，无残缺词、无重复选项、无整句挖空

## v0.8.1 — 2026-08-16

### Added
- 练习应用改造为 PWA，可安装到手机主屏幕：新增 `manifest.json`、`sw.js` 与 `icon-192/512/180.png`
- **离线做题**：service worker 缓存应用本体与题目数据；安装时预先缓存最新一天，首次访问后立刻断网也能用
- **离线结果暂存**：答完的结果先存在本机，联网后页面顶部提示条逐日提交；未提交就关闭页面也不会丢失

### Notes
- 缓存分两种策略：应用本体缓存优先（后台更新），题目数据网络优先（回落缓存），否则缓存的 `index.json` 会挡住当天新生成的题
- 图标 `毎日` 采用青绿渐变（`#34d399` → `#0891b2`），`theme_color` 同步；占宽 71%，位于 maskable 安全区（80%）内，Android 圆形遮罩不会裁切
- 修改应用文件后需将 `sw.js` 中的 `VERSION` 加一，旧缓存会在下次激活时清除

## v0.8.0 — 2026-08-16

### 重构
- 删除独立的 `handbook/08-Duolingo.md`，词汇不再使用独立的 `DUO-` 编号体系
- 已学词汇（经与 `02-Verbs.md` 交叉核对去重后）并入 `05-Vocabulary.md`，采用规范 `W-` 编号：
  - 名词 `W-N032` – `W-N086`（55 条）
  - 动词 `W-V023` – `W-V029`（7 条）
  - い形容词 `W-I008` – `W-I011`（4 条）
  - な形容词 `W-NA005` – `W-NA006`（2 条）
- 8 个动词（会う、住む、話す、作る、飲む、手伝う、忘れる、疲れる）已收录于 `02-Verbs.md` V-004 表，未重复创建 `W-V` 条目
- 删除尚未学习的 4 个主题共 31 词（地理与场所、科技、人际关系、学业与时事），不作为 Handbook 内容保留
- 更新 `handbook/99-Index.md`：移除 Duolingo 索引块，新增 68 条 Vocabulary 索引

### Changed
- 新增条目补全「常见搭配／区别」列：标注自他动词对（治る／治す、焼く／焼ける、混ぜる／混ざる）、易错助词（～に乗り換える、～に会う）、施动方向（診察を受ける／診察する）及易混淆词辨析
- `PROJECT_SPEC.md`、`README.md`、`SUMMARY.md`、`docs/README.md` 同步移除 08-Duolingo 相关条目
- `exercise-generator/itembank.py` 不再读取已删除的文件
- `AI_CONTENT_EXTRACTION_GUIDE.md` §3「不收录普通英语外来词」一条补充判断示例：`ホチキス`（和製英語）、`レポート`（日语特有用法）属例外可收录；`パスワード` 与英语原词完全对应，不收录。并说明该条只约束是否单独立条目，不限制外来词出现在例句中
- 提取流程自检清单相应增加一项
- `docs/README.md` 明确出题原则：例句可以使用题库外的词，但被考的对象必须是题库条目
- `PROJECT_SPEC.md` 新增 §5.1「各文件适用的语言」：原 §5「正文说明使用中文」未区分文件类型，与三份英文 AI 流程文档冲突；现明确手册内容与面向使用者的说明用中文，AI 流程文档沿用英文，并要求修改时沿用该文件既有语言

### Notes
- 编号映射记录于独立的 `MIGRATION_MAP.md`，不写入本文件与手册正文
- 本次属 `PROJECT_SPEC.md` 第 4 节第 7 条允许的结构迁移：提供完整映射、更新全部引用并在此记录
- `docs/data/` 下的历史答题记录保持原样不改写，编号对照见 `MIGRATION_MAP.md` 第四节

## v0.7.2 — 2026-08-16

### Changed
- 每日生成时间改为 **UTC 午夜**（`0 0 * * *`），练习日期改用运行机的 UTC 日期
- 删除原先为夏令时准备的双 cron 与「只保留 08:00 英国时间那一次」的判断步骤——锚定 UTC 后不再需要季节性修正

### Fixed
- 修正比较类条目（E-010、E-011、E-012）把 markdown 表头当作释义的问题；释义解析会跳过表格、分隔线、列表项与引用，并转成纯文本（去掉行内代码反引号、markdown 链接只保留文字）
- 新增两道防线：释义选择题拒绝「看起来像 markdown」的释义；质量检查在任何答案或选项混入 markdown 时判当天失败
- 修正提交守卫使用 `git diff` 导致未跟踪文件被忽略的问题（答题结果被静默丢弃），改用 `git status --porcelain`
- 修正 `VALID_ID_RE` 不接受 `VF-*` 编号的问题——V-004 动词表的 100 个条目此前会被当作非法 ID 丢弃
- 摄入工作流改为只监听 `labeled`，并按 issue 编号去重，避免同一次提交被记录两次
- 每日提交信息改用实际生成的日期，而非运行当天的日期

### Added
- `exercise-generator/check_id_pattern.py`：断言题库能产出的每个 ID 都被摄入端接受，并接入两个工作流

## v0.7.1 — 2026-08-15

### Added
- 练习题库新增两个此前未使用的章节来源：
  - `02-Verbs.md` V-004 速查表 → 100 个动词的变形题（`VF-001` – `VF-100`）
  - `02-Verbs.md` V-002 – V-008 → 各动词形式的知识条目
  - `06-Mistakes.md` M-001 – M-011 → 改错题
- 新增题型：改错题（Correction）与变形题（Conjugation）
- 新增易混淆关系解析：从手册的 `易混淆表达` 和 `Related` 小节提取，既识别编号也识别名称，用于挑选有迷惑性的干扰项
- 新增掌握度模型 `docs/data/mastery.json`：记录 correct_count、wrong_count、streak、last_seen、last_wrong、mastery
- 新增生成后质量检查：题目数量、答案完整性、选项去重、题型多样性、与近期题目的重复度

### Changed
- 选择题干扰项改为按「手册记录的易混淆项 → 同词性同主题 → 同词性 → 同类别」优先级挑选，不再随机取同类条目
- 变形题干扰项改为**同一动词的错误变形**（如 `遊ぶ` → `遊びて`、`開ける` → `開けって`），考查活用规则而非词汇眼熟度
- 选题改由掌握度驱动：掌握度低者优先，已掌握者 14 天后进入间隔复习
- 题型分配改为按「该知识点可行的题型」×「当天已用题型」平衡，避免整天都是选择题
- 同一知识点再次出现时优先更换例句或题型
- 自由作答题新增「接近但不同」的判定，不再一律判为错误

- 新增「回放自己写错的句子」：自由作答题答错时上报实际写的句子，之后该知识点优先出成针对该句的改错题（§5.1、§8.3）

### Notes
- `06-Mistakes.md` 中 `❌`（真正错误）与 `△`（语法正确但不自然）区别对待：前者出改错题，后者出「哪种说法更自然」，避免把不够自然的句子教成错误
- 判为「接近参考答案」的作答不会被当作错误回放——机械判分下这类写法往往本身正确
- 上报的句子会做长度限制和控制字符清理；公开仓库中错题原文对所有人可见
- 题库条目数从 244 增至 361

## v0.7.0 — 2026-08-15

### Added
- 新增 `handbook/08-Duolingo.md`：Duolingo Sections 1–4 补充词汇 `DUO-001` – `DUO-107`，按 Section 分组，独立于核心 `W-*` 编号体系
- 新增每日练习应用（`docs/`）：静态网页，支持选择题、填空题和中译日三种题型
- 新增练习生成脚本（`exercise-generator/`）：
  - `itembank.py` 解析 handbook Markdown 为结构化题库
  - `generate_exercises.py` 每天生成 5 道练习
  - `ingest_mistakes.py` 记录答题结果
- 新增错题复习机制：错题写入 `docs/data/mistakes.jsonl`，生成器每天预留 2 个名额优先复习；答对次数追平答错次数后自动退出复习队列
- 新增自动化工作流：`exercise-daily.yml` 每天生成练习，`exercise-ingest-results.yml` 记录答题结果

### Changed
- `PROJECT_SPEC.md` 第 3 节改为「仓库结构」，明确区分 **A. 手册内容** 与 **B. 练习应用** 两部分及其边界规则
- `README.md` 按 A／B 两部分重写仓库结构，并增加每日练习说明
- `SUMMARY.md` 增加 Duolingo 章节与练习应用入口
- `handbook/99-Index.md` 增加 Duolingo 词汇索引（按 Section 分组）
- 工作流文件统一使用 `exercise-` 前缀，明确归属于练习应用
- 每日生成时间改为英国时间早上 8 点，练习日期改用 `TZ=Europe/London`；因 GitHub cron 按 UTC 且不跟随夏令时，改为配置 `0 7` 与 `0 8` 两个 cron，并在首步判断伦敦当地时间，只保留正好 8 点的那一次

### Boundary rules
- 练习应用只读 `handbook/`，任何脚本都不写回手册内容（已核对：全部写入目标都在 `docs/data/` 下）
- `docs/data/` 是派生数据，可随时重新生成，不属于知识资产
- 只修改练习应用时不需要走 `RELEASE_WORKFLOW.md` 流程

### Notes
- Duolingo 未公开逐课时词表，且课程内容需登录查看，`08-Duolingo.md` 中的词汇按各 Section 已确认的主题范围整理，并已与 `05-Vocabulary.md` 去重；可直接编辑该表格替换为实际学习进度中的词汇
- 题库只收录带例句的条目：填空题与翻译题都依赖例句

### Validation
- 基线版本：v0.6.4
- 不修改任何已有知识点 ID
- 不修改 `handbook/01` – `handbook/07` 的内容
- 题库解析结果：244 个可出题条目（语法 37、助词 11、固定表达 14、词汇 182）
- 已在浏览器中验证网页应用的加载、判分、错题上报与日期切换
- 已验证错题回流闭环：答错 → 次日进入复习 → 答对后退出复习队列

## v0.6.4 — 2026-08-10

### Corrective
- 修正 PROJECT_SPEC.md 中过时的 Vocabulary `W-001` 编号说明
- 明确 Vocabulary 按词性使用 `W-N`、`W-V`、`W-I`、`W-NA`、`W-ADV`、`W-CON` 独立连续编号
- 修正 README.md 中对应的旧编号示例
- 澄清永久 ID 规则：默认禁止重编号；只有不可避免的结构迁移才允许，并要求保留旧锚点、完整映射、全部引用更新及 CHANGELOG 记录

### Validation
- 基线版本：v0.6.3
- 不修改任何已有知识点 ID
- 不修改 handbook/* 内容
- PROJECT_SPEC 与实际 Vocabulary 编号体系一致
- README 与 PROJECT_SPEC 编号示例一致

## v0.6.3 — 2026-08-10

### Added
- 新增 G-035 `～しか～ない`：限定“只有……”，以及 `しか` 后接否定的规则
- 新增 G-036 `～ことに気づく`：表示注意到、察觉到某个事实
- 新增 G-037 `い形容词的て形`：`～くて` 的连接用法
- 新增词汇：日付、入場料、お城、朝市、二人分、勘違い
- 新增动词：払う、違う、気づく、がっかりする、遠回りする
- 新增副词：全然、ただ
- 新增 M-010 `動詞＋のが好き`、M-011 `遠回りする的活用`
- 新增 R-007 `限定、发现与状态描述`

### Validation
- 基线版本：v0.6.2
- 基线提交：`975ea4036dfc7df7e364c077465dbf56a2e23379`
- 已扫描本轮日语学习对话并与当前仓库内容去重
- 新增 Grammar 条目均含至少两个自然例句、假名和中文翻译
- 新增 Vocabulary 行统一使用首列内固定锚点与 `<br>` 例句分隔
- 已按 `05-Vocabulary.md` 的真实 H2 章节边界定位词汇插入点
- 已检查新增 ID、Index 目标、重复锚点、表格列数和内部路径

# 更新日志 | Changelog

本文件记录《日语学习手册》的主要更新。

This file records the major updates to the Japanese Handbook.

---

## v0.6.2 — 2026-08-04

### Added
- 新增 G-033 `数量词＋ずつ`
- 新增 G-034 `定语从句中的が／の`
- 新增 P-011 `へ＋の → への`
- 新增 E-015 `ところで`
- 新增词汇：振り返る、付いてくる、ごまかす、多い、びっくり、確かに、ずっと
- 新增 R-006 `多い／多くない／少ない／少なくない`

### Validation
- 基线版本：v0.6.1
- 已扫描当前日语学习对话并与现有条目去重
- 新增语法均含至少两个自然例句、假名和中文翻译
- 新增 ID 使用永久锚点且未修改既有编号
- 已检查 Index 目标、重复锚点、UTF-8 和内部路径

---

## v0.6.1 — 2026-08-03

### Added
- 新增 E-009 `でも／しかし／けれど（けど）`
- 新增 E-010 `から／ので／だから／そのため`
- 新增 E-011 `そして／それから／それに／さらに`
- 新增 E-012 `例えば／など／とか`
- 新增 E-013 `それなら／だったら`
- 新增 E-014 `国名＋名词／国名の名词`
- 新增 R-005 连接表达比较

### Changed
- 更新 Expressions 和 Reviews 的可点击 Index
- 明确同组连接表达不能机械互换，应根据语法位置、逻辑关系和正式程度选择
- 补充 `スペイン映画` 与 `スペインの映画` 的类别／来源区别

### Validation
- 基线版本：v0.6.0
- 已扫描当前聊天并与仓库现有 Grammar、Particles 和 Expressions 去重
- 新增 ID 使用永久锚点并保持连续
- Review 答案包含完整日语句子、假名、中文翻译和解析
- 已检查 UTF-8、Index 链接、锚点唯一性和内部路径

---

## v0.6.0 — 2026-08-02

### Added
- 为所有 G、V、P、E、W、M、R 条目建立仅由永久编号组成的固定锚点
- 将 `handbook/99-Index.md` 全部知识条目改为可点击链接
- 在 PROJECT_SPEC 中确立“已发布编号是永久 ID”的长期规范
- 在发布验证中加入重复锚点、缺失目标和内部路径检查

### Changed
- 将现有 Related、Reviews 和 Common Mistakes 中可识别的编号引用改为可点击链接
- 更新 PROJECT_SPEC、RELEASE_WORKFLOW、AI_CONTENT_EXTRACTION_GUIDE 和 KNOWLEDGE_CLASSIFICATION
- 明确 Reviews 必须提供完整日语答案、假名、中文翻译和解析
- 明确 Common Mistakes 必须提供完整错误句和完整正确句

### Validation
- 基线版本：v0.5.4
- 未新增、删除或重新编号任何知识点
- 已验证 Index 中全部编号均有唯一锚点
- 已验证固定锚点不依赖知识点标题
- 已检查 UTF-8、重复锚点、缺失链接目标和内部文件路径

---

## v0.5.4 — 2026-08-02

### Added
- 新增10个高价值名词、10个动词、4个い形容词、3个な形容词和5个副词
- 新增自动词／他动词重点组：入る／入れる、付く／付ける、始まる／始める
- 新增 M-006 至 M-009
- 新增 R-003 自动词与他动词、R-004 高频易混词

### Changed
- 重写 M-001 至 M-005，加入完整错误句、正确句、翻译和原因
- 重写 R-001 和 R-002 的答案，确保包含完整日语句子、假名、翻译和解析
- 更新 Vocabulary、Common Mistakes、Reviews 和 Index

### Validation
- 基线版本：v0.5.3
- 已对照历史 conversations.json、近期日语学习对话和当前仓库去重
- 未收录普通英语外来词
- 已检查编号连续性、表格结构、例句完整性、UTF-8 编码和 Index 一致性

---

## v0.5.3 — 2026-08-01

### Added
- 新增 P-005 `を`
- 新增 P-006 `に`
- 新增 P-007 `で`
- 新增 P-008 `の`
- 新增 P-009 `も`
- 新增 P-010 `から／まで`

### Changed
- 更新 Particles Index，加入 P-005 至 P-010

### Validation
- 基线版本：v0.5.2
- 已检查新增内容与 P-001 至 P-004 不重复
- 已检查例句、编号、交叉引用和 UTF-8 编码

---

## v0.5.2 — 2026-07-31

### Added
- 新增 V-005 `た形`
- 新增 V-006 `ない形`
- 新增 V-007 `可能形`
- 新增 V-008 `意向形`

### Changed
- 更新 Verbs Index，加入 V-005 至 V-008

### Validation
- 基线版本：v0.5.1
- 已检查新增内容与 V-001 至 V-004 不重复
- 已检查变化规则、特殊变化、例句、编号、交叉引用和 UTF-8 编码

---

## v0.5.1 — 2026-07-31

### Added
- 新增 G-026 `～てみる`：尝试做某事
- 新增 G-027 `～ながら`：同时进行两个动作
- 新增 G-028 `～ないでください`：礼貌请求对方不要做某事
- 新增 G-029 `～ておく`：预先准备或保持状态
- 新增 G-030 `～ので`：较柔和、客观地说明原因
- 新增 G-031 `～たい`：表达自己的愿望
- 新增 G-032 `～んです／～のです`：解释背景、强调情况或询问原因

### Changed
- 更新 Grammar Index，加入 G-026 至 G-032

### Validation
- 基线版本：v0.5.0
- 知识来源：完整 `conversations.json` 日语学习聊天扫描
- 已与现有 G-001 至 G-025 去重
- 每个新增语法至少包含两个自然例句、适当假名和自然中文翻译
- 已检查编号连续性、交叉引用、UTF-8 编码和 Index 一致性
- 未新增英语外来词词汇条目

---

## v0.5.0 — 2026-07-30

### Added
- 新增 G-021 `～ても`：让步条件“即使……也……”
- 新增 G-022 `～ている／～ています`：进行、结果状态和习惯
- 新增 G-023 `～のに`：与期待相反的转折
- 新增 G-024 `～てもいい`：许可和询问许可
- 新增 G-025 `～てほしい`：希望别人做某事

### Changed
- 更新 `AI_CONTENT_EXTRACTION_GUIDE.md`：一般不收录普通英语外来词；只有存在日语特有含义、构词、对比或用法时才例外
- 更新 Grammar Index，加入 G-021 至 G-025

### Validation
- 基线版本：v0.4.2
- 已检查 G-021 至 G-025 与现有 G-001 至 G-020 不重复
- 每个新增语法至少包含两个自然例句、汉字假名和中文翻译
- 已检查编号连续性、交叉引用、UTF-8 编码和 Index 一致性
- 未新增英语外来词词汇条目

---

## v0.4.2 — 2026-07-30

### Added
- 新增 `AI_CONTENT_EXTRACTION_GUIDE.md`，规定 AI 如何从日语学习聊天中拆分、筛选、搜索、去重和改写知识点
- 新增 `KNOWLEDGE_CLASSIFICATION.md`，提供各章节的快速分类决策树

### Changed
- 更新 README 的仓库结构和 AI 必读文件清单
- 更新 PROJECT_SPEC，将聊天知识提取和分类规则指向两个独立规范文件
- 更新 RELEASE_WORKFLOW，将知识提取、分类和去重纳入正式发布流程

### Validation
- 基线版本：v0.4.1
- 全部新增和修改文件使用严格 UTF-8 编码
- 未修改 Handbook 章节、知识编号或 Index
- 已检查新增文档与 PROJECT_SPEC 的职责边界

---

## v0.4.1 — 2026-07-29

### Added
- 新增根目录 `RELEASE_WORKFLOW.md`，规定正式更新包的强制发布流程和 Release Gate
- 新增 E-006 一年中、E-007 もう一回、E-008 ～からもらったものです
- 新增词汇：大人、半日、男、男性、気持ち、気持ち悪い、ひどい、静か、わくわく
- 新增 M-004「一年中この映画を見たいです」和 M-005「大人の人」
- 新增 R-002 条件表达比较

### Changed
- 删除独立 Listening 章节；听力与阅读材料仅作为知识来源
- Reviews 从 `08-Reviews.md` 调整为 `07-Reviews.md`
- 明确 Reviews 只用于比较、复习和综合练习，不重复维护知识定义
- 更新 README、PROJECT_SPEC、SUMMARY 和 Index，使结构保持一致

### Validation
- 基线版本：v0.4.0
- 已检查 README、PROJECT_SPEC、SUMMARY、CHANGELOG 及所有受影响章节
- 已检查章节路径、编号连续性和索引一致性

---

## v0.4.0 — 2026-07-28

### Changed
- 重构 Verbs：由逐个动词条目改为系统讲解动词分类和活用
- Vocabulary 改为按词性分组的表格结构
- Vocabulary 使用分类编号：W-N、W-V、W-I、W-NA、W-ADV、W-CON
- 原 V-001 至 V-004 词条迁移到 Vocabulary 动词表

### Added
- V-001 动词分类
- V-002 ます形
- V-003 て形（规则、用途、例句和常见错误）
- V-004 常用100动词て形速查表

### ID Migration
- V-001 進む → W-V001 進む
- V-002 送る → W-V002 送る
- V-003 入れる → W-V003 入れる
- V-004 答える → W-V004 答える

### Fixed
- 同步更新 Verbs、Vocabulary 和 Index 的编号与交叉引用

---

## v0.3.0 — 2026-07-27

### Changed
- Grammar 按主题重新排序并连续编号为 G-001 至 G-020
- 更新全部旧 Grammar 编号映射
- 修正 PROJECT_SPEC 中"编号永久不变"与实际维护规则的冲突
- 明确每个 Grammar 条目至少包含两个自然例句

### Added
- G-004 ～前に
- G-008 ～ようにと言う
- G-013 ～のは
- G-014 ～ものだ／～ものです（定义、性质）
- G-015 普通形＋と思う
- G-017 引用助词「と」＋言う
- G-018 疑问句普通形＋か＋聞く
- G-019 ～てくれる

### Fixed
- 建立重编号时必须同步更新交叉引用和索引的规则

---

## v0.2.3 — 2026-07-27

### Changed
- Grammar 按主题重新编号（G-001 至 G-012）
- ～ように G-007→G-008，～ために G-008→G-007
- ～ようになる G-001→G-009，～たことがある G-003→G-010
- ～てくる G-004→G-002，～ていく G-005→G-003
- ～かもしれません G-006→G-011，～すぎる G-009→G-012
- ～たら G-010→G-005

### Added
- G-004 ～なら
- G-006 ～ば
- 更新 E-005 交叉引用（G-007→G-008）

---

## v0.2.2 — 2026-07-27

### 新增 | Added

- 补充 G-001 第三例句、G-003 第三例句、G-010 第三例句
- 补充 G-002 第二例句
- 新增 G-004、G-005、G-006、G-007、G-008 构成说明
- 补充 G-004 至 G-009 各补充一个例句
- 补充 V-002 五段动词活用规律说明
- 新增 P-001／P-002 は vs が 对比例句
- 扩充 E-001：新增构成说明和第二例句

---

## v0.2.1 — 2026-07-26

### 新增 | Added

- 新增语法：G-010 ～たら
- 补充 G-001 例句、G-003 例句
- 补充 V-001 た形活用
- 补充 P-001 时间主题用法
- 新增固定表达：E-004 ～何度も、E-005 ～分かりやすいように
- 新增词汇：W-006 上司、W-007 同僚、W-008 売上、W-009 レポート、W-010 ホチキス
- 更新 M-001，补充更自然的正确表达

---

## v0.2.0 — 2026-07-26

### 新增 | Added

- 创建 `handbook` 目录及九个章节
- 添加第一批语法、动词、助词、固定表达、词汇和易错点
- 添加听力技巧和阶段复习内容
- 创建永久编号索引
- 将 `SUMMARY.md` 更新为可点击目录

---

## v0.1.0 — 2026-07-26

### 新增 | Added

- 创建项目仓库
- 添加中英双语 README
- 建立项目规范文件
- 建立手册目录
- 建立更新日志
